os: Visual Studio 2017
platform: Any CPU
configuration: Release

branches:
  only:
    - master

before_build:
  - nuget restore

assembly_info:
  patch: true
  file: AssemblyInfo.cs
  assembly_version: $(appveyor_build_version)
  assembly_file_version: $(appveyor_build_version)

artifacts:
  - path: tailp\bin\$(configuration)\tailp.exe
    name: tailp
